{
  "mcpServers": {
    "forge-skills": {
      "command": "node",
      "args": ["scripts/forgeMcpServer.js"],
      "cwd": "skill-reputation",
      "env": {
        "CMC_USE_MOCK": "0"
      }
    }
  },
  "_note": "Forge Skills MCP — add to Cursor Settings → MCP. Tools: forge_list_skills, forge_get_backtest, forge_get_signals, forge_verify_submission, forge_agent_status, forge_bnb_stack. Requires CMC_API_KEY in skill-reputation/.env for live signals."
}
