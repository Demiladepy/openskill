# Garry Tan gstack clone — optional local tool, do not commit
gstack/

# Secrets — never commit real keys (examples *.env.example are OK)
.env
.env.*
**/.env
**/.env.agent
!**/.env.example
!**/.env.*.example

# TWAK session (never commit)
.twak/
**/.twak/

node_modules/
.next/
cache/
artifacts/
__pycache__/
*.pyc
